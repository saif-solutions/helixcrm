// Basic test setup file
// Add any global test configuration here

// Mock environment variables if needed
process.env.NODE_ENV = 'test';

// Add any global setup before tests
beforeAll(() => {
  // Add setup logic here
});

afterAll(() => {
  // Add cleanup logic here
});
