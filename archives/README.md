# Archived Test Files

This directory contains archived test files that were duplicates or moved during the test infrastructure cleanup (March 2026).

## Contents

- `duplicate-tests-*/` - Duplicate unit test files from old locations
- `old-test-folders-*/` - Original test folders after migration

These files are kept for reference only. The active test files are in `/tests/`.
